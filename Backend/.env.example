MONGO_URI=mongodb://127.0.0.1:27017/eduauth
JWT_SECRET=replace_with_strong_secret
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!
PORT=5000
# Note: On first start, the server will auto-create the super admin using ADMIN_* if it doesn't exist.
